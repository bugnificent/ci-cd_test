name: CI/CD with Unlighthouse

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  checks: write
  id-token: write

jobs:
  accessibility:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Unlighthouse
        run: npm install -g @unlighthouse/cli puppeteer netlify-cli

      - name: Run Unlighthouse scan
        run: |
         unlighthouse-ci --site ci-cdtest.vercel.app --build-static 
      - name: Run Unlighthouse Scan
        run: |
         # Varsayılan .unlighthouse klasörüne rapor oluştur
         unlighthouse-ci --site https://ci-cdtest.vercel.app --build-static
         # Debug: Oluşan dosyaları kontrol et
         echo "📂 Unlighthouse çıktıları:"
         ls -la ./.unlighthouse

      - name: Move Reports to SoftLand
        run: |
         # Raporları SoftLand'ın public dizinine taşı
         mkdir -p ./SoftLand/public/reports
         cp -r ./.unlighthouse/* ./SoftLand/public/reports/
         echo "✅ Raporlar taşındı:" && ls -la ./SoftLand/public/reports
